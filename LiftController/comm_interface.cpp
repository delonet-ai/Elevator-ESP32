#include "comm_interface.h"
#include <Arduino.h>

void commInit() {
  // Пока заглушка. Здесь будет ESP-NOW / пульт.
  Serial.println("[COMM] Init (stub)");
}

void commUpdate() {
  // Пока ничего, но каркас есть.
}
